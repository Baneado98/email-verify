# Smithery configuration for email-verify
# https://smithery.ai/docs
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: "node",
      args: ["dist/mcp.js"]
    })
build:
  dockerfile: Dockerfile
